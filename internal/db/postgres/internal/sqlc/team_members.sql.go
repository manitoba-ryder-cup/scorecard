// Code generated by sqlc. DO NOT EDIT.
// versions:
//   sqlc v1.30.0
// source: team_members.sql

package sqlc

import (
	"context"

	"github.com/google/uuid"
)

const createTeamMember = `-- name: CreateTeamMember :one
INSERT INTO team_members (
    tournament_id,
    player_id,
    team_id,
    tenant_id,
    is_captain
) VALUES (
    $1, $2, $3, $4, $5
) RETURNING tournament_id, player_id, team_id, tenant_id, is_captain
`

type CreateTeamMemberParams struct {
	TournamentID int32     `json:"tournament_id"`
	PlayerID     int32     `json:"player_id"`
	TeamID       int32     `json:"team_id"`
	TenantID     uuid.UUID `json:"tenant_id"`
	IsCaptain    bool      `json:"is_captain"`
}

func (q *Queries) CreateTeamMember(ctx context.Context, arg CreateTeamMemberParams) (TeamMember, error) {
	row := q.db.QueryRow(ctx, createTeamMember,
		arg.TournamentID,
		arg.PlayerID,
		arg.TeamID,
		arg.TenantID,
		arg.IsCaptain,
	)
	var i TeamMember
	err := row.Scan(
		&i.TournamentID,
		&i.PlayerID,
		&i.TeamID,
		&i.TenantID,
		&i.IsCaptain,
	)
	return i, err
}

const deleteTeamMember = `-- name: DeleteTeamMember :exec
DELETE FROM team_members
WHERE tournament_id = $1 AND player_id = $2 AND tenant_id = $3
`

type DeleteTeamMemberParams struct {
	TournamentID int32     `json:"tournament_id"`
	PlayerID     int32     `json:"player_id"`
	TenantID     uuid.UUID `json:"tenant_id"`
}

func (q *Queries) DeleteTeamMember(ctx context.Context, arg DeleteTeamMemberParams) error {
	_, err := q.db.Exec(ctx, deleteTeamMember, arg.TournamentID, arg.PlayerID, arg.TenantID)
	return err
}

const getTeamCaptain = `-- name: GetTeamCaptain :one
SELECT tm.tournament_id, tm.player_id, tm.team_id, tm.tenant_id, tm.is_captain, p.first_name, p.last_name, p.email
FROM team_members tm
JOIN players p ON tm.player_id = p.id
WHERE tm.tournament_id = $1 AND tm.team_id = $2 AND tm.is_captain = true AND tm.tenant_id = $3
LIMIT 1
`

type GetTeamCaptainParams struct {
	TournamentID int32     `json:"tournament_id"`
	TeamID       int32     `json:"team_id"`
	TenantID     uuid.UUID `json:"tenant_id"`
}

type GetTeamCaptainRow struct {
	TournamentID int32     `json:"tournament_id"`
	PlayerID     int32     `json:"player_id"`
	TeamID       int32     `json:"team_id"`
	TenantID     uuid.UUID `json:"tenant_id"`
	IsCaptain    bool      `json:"is_captain"`
	FirstName    string    `json:"first_name"`
	LastName     string    `json:"last_name"`
	Email        string    `json:"email"`
}

func (q *Queries) GetTeamCaptain(ctx context.Context, arg GetTeamCaptainParams) (GetTeamCaptainRow, error) {
	row := q.db.QueryRow(ctx, getTeamCaptain, arg.TournamentID, arg.TeamID, arg.TenantID)
	var i GetTeamCaptainRow
	err := row.Scan(
		&i.TournamentID,
		&i.PlayerID,
		&i.TeamID,
		&i.TenantID,
		&i.IsCaptain,
		&i.FirstName,
		&i.LastName,
		&i.Email,
	)
	return i, err
}

const getTeamMember = `-- name: GetTeamMember :one
SELECT tournament_id, player_id, team_id, tenant_id, is_captain FROM team_members
WHERE tournament_id = $1 AND player_id = $2 AND tenant_id = $3
`

type GetTeamMemberParams struct {
	TournamentID int32     `json:"tournament_id"`
	PlayerID     int32     `json:"player_id"`
	TenantID     uuid.UUID `json:"tenant_id"`
}

func (q *Queries) GetTeamMember(ctx context.Context, arg GetTeamMemberParams) (TeamMember, error) {
	row := q.db.QueryRow(ctx, getTeamMember, arg.TournamentID, arg.PlayerID, arg.TenantID)
	var i TeamMember
	err := row.Scan(
		&i.TournamentID,
		&i.PlayerID,
		&i.TeamID,
		&i.TenantID,
		&i.IsCaptain,
	)
	return i, err
}

const listTeamMembersByTeam = `-- name: ListTeamMembersByTeam :many
SELECT tm.tournament_id, tm.player_id, tm.team_id, tm.tenant_id, tm.is_captain, p.first_name, p.last_name, p.email
FROM team_members tm
JOIN players p ON tm.player_id = p.id
WHERE tm.tournament_id = $1 AND tm.team_id = $2 AND tm.tenant_id = $3
ORDER BY p.last_name, p.first_name
`

type ListTeamMembersByTeamParams struct {
	TournamentID int32     `json:"tournament_id"`
	TeamID       int32     `json:"team_id"`
	TenantID     uuid.UUID `json:"tenant_id"`
}

type ListTeamMembersByTeamRow struct {
	TournamentID int32     `json:"tournament_id"`
	PlayerID     int32     `json:"player_id"`
	TeamID       int32     `json:"team_id"`
	TenantID     uuid.UUID `json:"tenant_id"`
	IsCaptain    bool      `json:"is_captain"`
	FirstName    string    `json:"first_name"`
	LastName     string    `json:"last_name"`
	Email        string    `json:"email"`
}

func (q *Queries) ListTeamMembersByTeam(ctx context.Context, arg ListTeamMembersByTeamParams) ([]ListTeamMembersByTeamRow, error) {
	rows, err := q.db.Query(ctx, listTeamMembersByTeam, arg.TournamentID, arg.TeamID, arg.TenantID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListTeamMembersByTeamRow{}
	for rows.Next() {
		var i ListTeamMembersByTeamRow
		if err := rows.Scan(
			&i.TournamentID,
			&i.PlayerID,
			&i.TeamID,
			&i.TenantID,
			&i.IsCaptain,
			&i.FirstName,
			&i.LastName,
			&i.Email,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const listTeamMembersByTournament = `-- name: ListTeamMembersByTournament :many
SELECT tm.tournament_id, tm.player_id, tm.team_id, tm.tenant_id, tm.is_captain, p.first_name, p.last_name, p.email, t.name as team_name
FROM team_members tm
JOIN players p ON tm.player_id = p.id
JOIN teams t ON tm.team_id = t.id
WHERE tm.tournament_id = $1 AND tm.tenant_id = $2
ORDER BY t.name, p.last_name, p.first_name
`

type ListTeamMembersByTournamentParams struct {
	TournamentID int32     `json:"tournament_id"`
	TenantID     uuid.UUID `json:"tenant_id"`
}

type ListTeamMembersByTournamentRow struct {
	TournamentID int32     `json:"tournament_id"`
	PlayerID     int32     `json:"player_id"`
	TeamID       int32     `json:"team_id"`
	TenantID     uuid.UUID `json:"tenant_id"`
	IsCaptain    bool      `json:"is_captain"`
	FirstName    string    `json:"first_name"`
	LastName     string    `json:"last_name"`
	Email        string    `json:"email"`
	TeamName     string    `json:"team_name"`
}

func (q *Queries) ListTeamMembersByTournament(ctx context.Context, arg ListTeamMembersByTournamentParams) ([]ListTeamMembersByTournamentRow, error) {
	rows, err := q.db.Query(ctx, listTeamMembersByTournament, arg.TournamentID, arg.TenantID)
	if err != nil {
		return nil, err
	}
	defer rows.Close()
	items := []ListTeamMembersByTournamentRow{}
	for rows.Next() {
		var i ListTeamMembersByTournamentRow
		if err := rows.Scan(
			&i.TournamentID,
			&i.PlayerID,
			&i.TeamID,
			&i.TenantID,
			&i.IsCaptain,
			&i.FirstName,
			&i.LastName,
			&i.Email,
			&i.TeamName,
		); err != nil {
			return nil, err
		}
		items = append(items, i)
	}
	if err := rows.Err(); err != nil {
		return nil, err
	}
	return items, nil
}

const updateTeamMemberCaptain = `-- name: UpdateTeamMemberCaptain :one
UPDATE team_members
SET is_captain = $4
WHERE tournament_id = $1 AND player_id = $2 AND tenant_id = $3
RETURNING tournament_id, player_id, team_id, tenant_id, is_captain
`

type UpdateTeamMemberCaptainParams struct {
	TournamentID int32     `json:"tournament_id"`
	PlayerID     int32     `json:"player_id"`
	TenantID     uuid.UUID `json:"tenant_id"`
	IsCaptain    bool      `json:"is_captain"`
}

func (q *Queries) UpdateTeamMemberCaptain(ctx context.Context, arg UpdateTeamMemberCaptainParams) (TeamMember, error) {
	row := q.db.QueryRow(ctx, updateTeamMemberCaptain,
		arg.TournamentID,
		arg.PlayerID,
		arg.TenantID,
		arg.IsCaptain,
	)
	var i TeamMember
	err := row.Scan(
		&i.TournamentID,
		&i.PlayerID,
		&i.TeamID,
		&i.TenantID,
		&i.IsCaptain,
	)
	return i, err
}
