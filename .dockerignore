# Version control and metadata
.git/
.github/
.gitignore

# Build artifacts
bin/

# Development config
.golangci.yaml
sqlc.yaml
**/Dockerfile*

# Docs
LICENSE
README.md

# Test suite and its generated JWT keys — not needed to build the binary, and the
# keys would otherwise land in the builder layer (and the CI layer cache).
test/
coverage.*
