// Package sdk defines the scorecard HTTP API contract: the request/response DTOs
// and route constants shared by the server, SDK clients, and integration tests.
// JSON is snake_case, and internal fields (e.g. tenant_id) are never exposed.
package sdk
